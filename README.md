# WeChat Connect
Bridge your local Claude Code CLI to WeChat. You can chat with your AI coding agent directly from WeChat.
## Setup
1. Install dependencies: `npm install`
2. Build project: `npm run build`
3. Setup WeChat credentials: `npm run weixin:login` (Scan QR code)
4. Start: `npm run dev`
